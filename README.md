# PDF to HTML Menus

Convert restaurant PDF menus into clean, responsive HTML/CSS with schema markup.

Features:
- PDF text extraction
- Menu section detection
- HTML/CSS generation
- Dashboard + history
- Regenerate + download
- Hosted embed script
